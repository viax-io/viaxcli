[package]
name = "query"
version = "0.1.0"
edition = "2021"

[dependencies]
cynic = { version = "3" }
viax-schema = { path = "../schema" }

[build-dependencies]
cynic-codegen = { version = "3", features = ["rkyv"] }
